info:
  app:
    name: ${spring.application.name}
  company:
    name: nohi
  build:
    artifactId: @project.artifactId@
    version: @project.version@

spring:
  application:
    name: @project.artifactId@
  cloud:
    nacos:
      config:
        server-addr: home.nohi.online:28848
        group: cloud-alibaba
        encode: utf-8
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: "*"

